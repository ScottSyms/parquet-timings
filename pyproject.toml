[project]
name = "ais-parquet-benchmark"
version = "0.1.0"
description = "Benchmark monolithic vs partitioned vs partitioned+bloom parquet formats for AIS data"
requires-python = ">=3.12"
dependencies = [
    "pyarrow>=16.0.0",
    "pytz",
]
